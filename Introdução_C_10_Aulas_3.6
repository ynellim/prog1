#include <stdio.h>
#include <stdlib.h>
                          //sequencia de fibonacci.

int main()
{
    int i,fib1=1, fib2=1, aux;

       printf("%d\n", fib1);
       printf("%d\n", fib2);

    for(i=1; i<20; i++){
       aux = fib2;
       fib2 += fib1;


       printf("%d\n", fib2);
       fib1 = aux; // nao entendi esse ponto
    }

return 0;
}
